flows:
  ##
  # Orchestrates the application deployment process.
  # Scans the artifact repository and pushes to the target cluster.
  #
  # in:
  #   cluster: string, mandatory, The target K8s cluster
  #   namespace: string, optional, Namespace (defaults to 'default')
  #   replicas: int, optional, Number of pods to spin up
  # out:
  #   deployId: string, mandatory, The generated deployment ID
  ##
  deployApp1:
    - log: "Deploying to ${cluster}/${namespace}..."
    - set:
        deployId: "test-deployment-id"
