1. 🚀 Productivity & Editing (Самое вкусное)
    * Language Injection (Внедрение языков):
        * Это, пожалуй, killer-feature, которой сейчас не хватает. Concord часто использует скриптовые вставки.
        * Сделай так, чтобы внутри полей cmd, script, expr (или где там используется Groovy/Javascript/Bash) IDE автоматически подсвечивала синтаксис соответствующего языка, давала
          автокомплит и инспекции.
        * Реализация: MultiHostInjector.
    * Live Templates (Живые шаблоны):
        * Набор сниппетов для быстрой вставки типовых конструкций: "Новый таск", "Цикл", "Try-catch блок", "Form block".
        * Например, пишешь task + Tab -> разворачивается полная структура таска.
    * Inlay Hints (Подсказки типов/имен):
        * Если Concord позволяет передавать аргументы позиционно или есть неявные параметры, подсказки прямо в редакторе (как в Java/Kotlin) будут очень кстати.

2. 🏃 Execution & Debugging
* Run Configuration (Запуск из IDE):
    * Добавь возможность запускать flow локально через Concord CLI (если он есть) или отправлять его на сервер прямо из IDE.
    * Кнопка "Run" (зеленый треугольник) напротив configuration или flows.
* Log Analyzer / Console Linker:
    * Если запуск реализован, то кликабельные ссылки в консоли логов, ведущие обратно к шагу в YAML файле, который упал.

3. 🗺 Visualization & Structure
* Flow Visualizer (Графическое представление):
    * Сгенерировать диаграмму (graphviz/plantuml или свой UI) на основе YAML. Пайплайны бывают запутанными, и увидеть граф переходов визуально — бесценно.
    * Можно сделать отдельной вкладкой в редакторе (как Markdown Preview).
* Breadcrumbs (Хлебные крошки):
    * Специализированные breadcrumbs внизу редактора, которые показывают не просто структуру YAML, а логический путь: MyFlow > Task: Deploy > Inputs.

4. 🧙‍♂️ UX & Onboarding
* New Project/File Wizard:
    * Пункт в File > New > Concord Project или Concord File.
    * Не просто пустой файл, а диалог, где можно выбрать шаблон (Basic, K8s deploy, Approval flow и т.д.).
* Documentation Lookup (Quick Doc):
    * Ты уже сделал DocumentationProvider, но круто было бы подтягивать доку по стандартным плагинам Concord (ansible, git и т.д.) прямо с официального сайта или из зашитых
      ресурсов при нажатии F1.

5. 🛡 Security & Quality
* Secret Management Helper:
    * Инспекция, которая ругается, если видит хардкод паролей/токенов в YAML и предлагает вынести их в секреты Concord.
* Dry Run / Validation Action:
    * Action для валидации файла целиком без запуска (если Concord умеет делать dry-run).

С чего я бы начал?
Однозначно Language Injection для Bash/Groovy. Это мгновенно поднимает UX на новый уровень, так как большую часть времени инженеры пишут именно скрипты внутри YAML.

Техническая реализация (Roadmap):

1. XDebugger SDK (Сторона IntelliJ)
   Тебе нужно реализовать расширения com.intellij.xdebugger:
* XDebugProcess: Управляет процессом отладки (Resume, Step Over, Stop).
* XLineBreakpointType: Разрешает ставить точки в .concord.yaml файлах.
* XValue / XValueGroup: Для отображения дерева переменных в окне Debug.

2. The Runner (Сторона Concord)
   Тебе нужен свой wrapper над concord-cli или использование concord-server в embedded режиме (если либы позволяют).
   Этот Runner должен уметь:
* Принимать список брейкпоинтов (имена файлов + номера строк) при старте.
* Иметь Event Listener (в Concord API наверняка есть хуки beforeTask, afterTask).

3. Протокол общения (IPC)
   Плагин и Runner — это два разных процесса. Им нужно общаться.
* Простой вариант: Runner открывает Socket. Плагин коннектится.
* Сценарий:
    1. Runner доходит до таска.
    2. Проверяет, есть ли брейкпоинт на этой строке (маппинг строки YAML в объект Flow сложный, но возможный, обычно у Node в AST есть location).
    3. Если есть брейкпоинт -> Runner "замораживается" (ждет команды в сокете) и шлёт Плагину JSON с текущим Контекстом.
    4. Плагин рисует переменные.
    5. Пользователь жмет "Step Over" (F8).
    6. Плагин шлёт команду "Resume" в сокет.


- инспекция для двойного блока flow doc
- docs
- fixed triggers version = 2
- resolve imports
- resolve tasks
- validate tasks input params
- validate flows input params
- validate script dependencies
